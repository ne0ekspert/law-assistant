# Anthropic chat with local embeddings (recommended)
ANTHROPIC_API_KEY=

# Providers
# Use local embeddings for speed/cost; switch to `anthropic` if your account has embeddings API.
EMBEDDINGS_PROVIDER=ollama
EMBEDDINGS_MODEL=nomic-embed-text
CHAT_PROVIDER=anthropic
CHAT_MODEL=claude-3-5-sonnet-20240620

